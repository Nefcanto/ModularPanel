import { PlaceForm } from "Places"

const WarehouseForm = props => {

    return <PlaceForm
        entityType="AdminWarehouse"
        humanReadableEntityType="InventoryWarehouse"
    />
}

export default WarehouseForm
