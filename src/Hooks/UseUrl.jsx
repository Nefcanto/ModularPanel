import { useMemo } from "react"
import {
    useLocation,
    useNavigate,
    useParams,
} from "react-router"
import app from "App"

const useUrl = () => {

    const navigate = useNavigate()

    return {
    }
}

export default useUrl
