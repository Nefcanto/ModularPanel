import { Enum } from "List"

const filters = <>
    <Enum
        entityType="CommentState"
        placeholder="SocialCommentState"
        property="CommentStateId"
    />
</>

export default filters
