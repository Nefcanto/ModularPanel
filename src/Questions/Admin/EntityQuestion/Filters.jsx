import {
    Enum,
    Title,
} from "List"

const filters = <>
    <Title />
    <Enum
        entityType="QuestionState"
        placeholder="QuestionsState"
        property="StateKey"
    />
</>

export default filters
