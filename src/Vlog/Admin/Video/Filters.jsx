import {
    Enum,
    Title,
} from "List"

const filters = <>
    <Title />
    <Enum
        property="State"
        placeholder="VlogState"
        entityType="VlogState"
    />
</>

export default filters
